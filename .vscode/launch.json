{
  "version": "0.2.0",
  "configurations": [
    {
        "command": "npm run develop",
        "name": "debug",
        "request": "launch",
        "autoAttachChildProcesses": true,
        "type": "node-terminal",
        "cwd": "${workspaceFolder}",
        "envFile": "${workspaceFolder}/.env"
    }
  ]
}